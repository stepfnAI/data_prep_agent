from setuptools import setup, find_packages

setup(
    name="sfn_agents_pipeline",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add your requirements here
    ]
) 